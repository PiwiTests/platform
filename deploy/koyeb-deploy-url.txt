# Generated by apps/application/scripts/generate-deploy-manifests.mjs — edit the script, not this file.
# Piwi Dashboard — one-click deploy. Docs: https://piwitests.dev/deployment
# Tracks :latest. Pin a version tag before you rely on it — migrations are forward-only.

https://app.koyeb.com/deploy?type=docker&image=docker.io/phenx/piwitests-server:latest&name=piwi&instance_type=small&regions=fra&ports=3000;http;/&env[PIWI_AUTH_ENABLED]=true

# Koyeb volumes cannot be attached from this URL, and without one the database and every
# stored trace are lost on redeploy. Attach one before the instance holds anything you want
# to keep (volumes are region-scoped, and only standard instances can mount them):
#
#   koyeb volumes create piwi-data --region fra --size 10
#   koyeb service update piwi/piwi --volumes piwi-data:/app/.data
#
# The button URL carries no health check either — Koyeb defaults to a TCP probe on the
# port. Switch it to HTTP GET /api/health, which also verifies database connectivity:
#
#   koyeb service update piwi/piwi --checks 3000:http:/api/health
#
# Set in the Koyeb console: PIWI_AUTH_SECRET, PIWI_SECRET_KEY, PIWI_SITE_URL. Generate each secret with: openssl rand -hex 32.
# Authentication is enabled above, so the service stays down until PIWI_AUTH_SECRET is set.
