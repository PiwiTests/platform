import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Extends the default VitePress theme with a small amount of custom CSS
// (see custom.css) — used to style inline screenshot figures on content pages.
export default DefaultTheme
