{
  "name": "piwi-dashboard-docs",
  "private": true,
  "scripts": {
    "docs:gen": "node scripts/generate-configuration.mjs && node scripts/generate-features.mjs && node scripts/generate-whats-new.mjs",
    "docs:dev": "npm run docs:gen && vitepress dev",
    "docs:build": "npm run docs:gen && vitepress build",
    "docs:preview": "vitepress preview"
  },
  "devDependencies": {
    "jiti": "^2.4.2",
    "vitepress": "^1.6.3"
  }
}
