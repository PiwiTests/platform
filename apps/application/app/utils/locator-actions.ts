/**
 * Readable names for the action keys the locator index stores
 * (`selectOption`, `expect.not.toBeVisible`, …), in sentence case. Shared with
 * the browser extension through `@piwitests/core`.
 */
export { locatorActionLabel } from '#shared/locator-chain';
